# Authenticate Wrangler with its local OAuth login. Do not store account
# identifiers or API tokens in project files.

# Required Cloudflare Access configuration for the Worker API. Keep real
# values in Wrangler secrets, never in this file.
TEAM_DOMAIN=https://your-team.cloudflareaccess.com
POLICY_AUD=
